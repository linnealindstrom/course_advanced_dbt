sources:
  - name: ads_platform
    schema: bingeflix
    description: This source contains tables from the Bingeflix ads platform database
    database: raw
    tables:
      - name: daily_ads
        identifier: ads
        description: This table contains information about daily ads on Bingeflix.
        columns:
          - name: campaign_id
            description: The unique identifier of the campaign_id.
            tests:
              - not_null
